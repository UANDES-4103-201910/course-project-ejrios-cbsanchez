<%= form_with(model: super_admin, local: true) do |form| %>
  <% if super_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(super_admin.errors.count, "error") %> prohibited this super_admin from being saved:</h2>

      <ul>
      <% super_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="field">
    <%= form.label :nickname %>
    <%= form.text_field :nickname %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :e_password %>
    <%= form.text_field :e_password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
