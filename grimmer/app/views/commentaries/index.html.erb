<p id="notice"><%= notice %></p>

<h1>Commentaries</h1>

<table>
  <thead>
    <tr>
      <th>Texto</th>
      <th>Country</th>
      <th>City</th>
      <th>User</th>
      <th>Post</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @commentaries.each do |commentary| %>
      <tr>
        <td><%= commentary.texto %></td>
        <td><%= commentary.country %></td>
        <td><%= commentary.city %></td>
        <td><%= commentary.user %></td>
        <td><%= commentary.post %></td>
        <td><%= link_to 'Show', commentary %></td>
        <td><%= link_to 'Edit', edit_commentary_path(commentary) %></td>
        <td><%= link_to 'Destroy', commentary, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Commentary', new_commentary_path %>
